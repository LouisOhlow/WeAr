import { StyleSheet } from 'react-native';

const textStyles = StyleSheet.create({
  basic: {
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  header: {
    color: 'white',
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default textStyles;
